/**
 * Date format types
 * - "YYYY/MM/DD HH:MM" - Full date with time
 * - "YYYY/MM/DD" - Date only (no time)
 * - "MM/DD HH:MM" - Month/day with time (no year)
 */
export type DateFormat = "YYYY/MM/DD HH:MM" | "YYYY/MM/DD" | "MM/DD HH:MM";
