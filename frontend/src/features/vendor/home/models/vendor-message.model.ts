// =============================================================================
// VENDOR MESSAGE MODEL
// =============================================================================

// Re-export from shared types (single source of truth)
export type {
  VendorMessage,
  VendorThreadMessage,
  VendorMessageThread,
} from "../../shared/models";
