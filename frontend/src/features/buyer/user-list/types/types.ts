// =============================================================================
// USER LIST TYPES
// =============================================================================

// Type aliases
export type UserPermission = "admin" | "member";
export type PermissionChangeModalState = "select" | "complete";

// Re-export models
export * from "../models";
