// =============================================================================
// ARCHIVE PROJECT MODEL
// =============================================================================

export interface ArchiveProject {
  id: string;
  name: string;
  authorId: string;
  authorName: string;
  createdAt: string;
  isFavorite: boolean;
}
